[project]
name = "mos-agondev"
version = "0.1.0"
description = "AgonDev build and compatibility tooling for Agon MOS"
requires-python = ">=3.14"
dependencies = []

[tool.unittest]
start-directory = "tests"
