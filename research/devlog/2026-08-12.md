# Development log — 2026-08-12

## Accepted implementation gates

1. **DL-021 / PORT-103:** Replaced the experimental C shims with a pinned,
   audited compatibility contract. The build now consumes nine hashed official
   AgonDev headers, exposes only the 44 hardware names actually used by MOS,
   checks register addresses, types, dependencies, I/O instructions, the
   32-bit FatFS timestamp shift, and `quickrand`'s explicit A-register return
   and zero extension. All 16 C units compile with return-type diagnostics as
   errors; the exact 67-file prepared worktree passes a read-only provenance
   check. PORT-103 was accepted and removed from `TODO.md`.
2. **DL-022 / PORT-101:** Accepted the strict ongoing ZDS compatibility
   frontend on its documented language surface. It preserves `$name` and
   `name?` scoped labels, anonymous labels, and macro locals without
   globalizing maintained source; textually expands safe includes; supports the
   audited directives, macros, literals, data widths, and public structure
   layout; records deterministic input/output provenance; and remaps GNU-as
   diagnostics to original root or include locations. Positive/negative
   goldens, path-safety cases, object bytes, binding, relocations, disassembly,
   structures, and the full deterministic corpus pass. All 15 build-critical
   roots route through the strict wrapper and link into the verified firmware,
   so PORT-101 was accepted and removed from `TODO.md`.

## Expanded parity and upstream findings

3. **DL-023 / PORT-201 partial:** Expanded the differential Fab CLI gate to an
   immutable four-file hostfs fixture and exact normalized transcript
   comparison with the pinned ZDS ROM. It now covers HELP/ECHO dispatch,
   SET/SHOW/UNSET state, RTC display, credits, root and nested DIR/CD/TYPE
   behavior, exact file bytes, missing paths/files, and invalid commands.
   Graphical keyboard/VDP, UART, write-side FatFS, RTC mutation, oversized VDP
   packets, and hardware remain open.
4. **DL-024 / PORT-202 partial:** The runtime host golden now covers every one
   of the 16 maintained formatter spellings. An AgonDev target MOSlet links the
   exact firmware formatter and exercises 24-bit integer boundaries, 32-bit
   long boundaries, widths/precision/padding/newline behavior, signed and
   unsigned status returns, high-byte 24-bit arguments, 32-bit seek/tell paths,
   pointer outputs, direct RST wrappers, delegated MOS C functions, and
   read-only FatFS against both candidate and ZDS firmware. The snapshotted
   temporary hostfs must remain byte-for-byte unchanged. Broad public ABI
   coverage remains PORT-202.
5. **DL-025 / PORT-204:** A source-wide audit of all 123 pinned AgonDev libmos
   wrappers found three concrete stack-access defects: `mos_getError` uses
   packed IX+7/IX+10 offsets instead of three-byte slot starts IX+9/IX+12;
   `ffs_setlabel` overwrites its valid HL argument through an uninitialized IX;
   and `mos_flseek_p` points to SP+9 rather than the low 32-bit word at SP+6.
   Minimal local wrappers, compiler call-site evidence, archive/source
   disassembly, and safe runtime cases verify the analysis. An upstream-ready
   report is in `docs/upstream-findings.md`; no upstream checkout was changed.
6. **DL-026:** The same target probe isolated a Fab directory-hostfs limitation:
   file open writes 32-bit `FIL.obj.objsize` with `_poke24`, truncating files
   above 16 MiB and consequently affecting `fsize`/`feof`. Candidate and ZDS
   MOS behave identically. The upstream-ready report distinguishes this from a
   MOS or AgonDev defect; the contract uses the sparse file only for 32-bit
   seek/tell/read and a small file for size/EOF.

## Verification and environment event

7. **DL-027:** A clean `make -j4 verify` passed 74 root tests, pinned-environment
   and 67-file provenance checks, source audit, toolchain and 16-unit C probes,
   C compatibility, all 15 strict assembly units, production linker rejection
   fixtures, restricted runtime closure and 16-spelling formatter golden,
   102,059-byte firmware verification, stock-profile integrity, custom hostfs
   boot, expanded ZDS shell parity, linked-byte VDP regressions, the 123-wrapper
   source audit, and the 42-wrapper target contract on both firmware images.
   At this point PORT-106 remained the required graphical human gate, so the
   emulator-coupled batch remained intentionally uncommitted.
8. **DL-028:** During the run, the host root filesystem remounted read-only.
   All upstream and original repository sources remained intact. Work continued
   in a temporary writable recovery clone; source and generated checks pass
   there, but the recovery changes must be reconciled into the canonical
   checkout after the host filesystem is repaired. This environment event does
   not weaken the build evidence and does prevent a canonical commit now.
9. **DL-029 / PORT-201 partial:** Added a fail-closed ADL interpreter that
   executes the actual linked `vdp_protocol` bytes for every oversized length
   from 17 through 255. It proves exact discard count, no packet-buffer or
   handler effects, return to idle, following-general-poll recovery, and the
   legal 16-byte boundary. The linked startup also retains a blocking GP poll
   before the boot banner; the headless banner is therefore bounded evidence
   that the stock VDP completed that handshake. The pinned ZDS image predates
   the source fix and is required to reproduce the historical stale-length bug
   as a behavioral negative control. Broader graphical/VDP/UART behavior stays
   open under PORT-201 and PORT-106 at this point.
10. **DL-030 / PORT-202 partial:** Expanded the immutable-hostfs target MOSlet
    to a 42-wrapper audited inventory on both candidate and ZDS firmware. The
    added read-only cases cover string extraction, GS state, argument
    substitution, six-argument path resolution, simple and structured file
    reads, pointer returns, stat, directory enumeration/matching, cwd, and
    volume label lookup. A `mos_extractnumber` call passes identically when
    isolated in a small target frame but produced a candidate-only failure when
    embedded in the probe's much larger frame; that unexplained frame
    sensitivity remains explicitly open under PORT-202 and is not attributed
    to MOS or an upstream wrapper without a minimal reproducer.
11. **DL-031 / PORT-106:** The author interactively qualified the custom
    AgonDev-built firmware with the stock Platform VDP: banner and prompt,
    keyboard editing, directory-backed SD, and the representative command set
    all passed. Fab's documented host-side RightCtrl-Q shortcut did not quit on
    this input stack; that UI binding is outside MOS and does not weaken the
    firmware result. The author then explicitly requested commit and public
    publication. Upstream worktrees remained clean, so PORT-106 was accepted
    and removed from `TODO.md`.
12. **DL-032:** Separated current onboarding and normative technical contracts
    from the original research narrative. The feasibility study, pinned précis,
    and dated logs moved under `research/`; fresh-clone and normal development
    instructions now live in `STARTHERE.md`. Project-local dependency defaults
    no longer assume the author's sibling-checkout layout. Portable
    configured-input verification is distinct from exact frozen-baseline
    reproduction, and PORT-205 records the remaining public-input gap.
13. **DL-033:** Hardened that separation before handoff. Prepared MOS trees now
    contain a fail-closed provenance sidecar with the actual source `HEAD`,
    tracked-dirty state, and every copied file's hash and executable bits; the
    assembly frontend validates it instead of stamping the frozen study commit
    onto ongoing-development builds. `make verify` checks the configured
    candidate without requiring path-dependent Fab hashes or the old ZDS VDP
    negative control, while `make baseline-check` adds those exact research
    identities and measurements. Project-local and explicit external Fab paths,
    including relative Make overrides, are covered by tests. The final isolated
    review passed 89 root tests, a clean 102,059-byte firmware rebuild, boot,
    shell parity, candidate VDP regression, the 42-wrapper target contract, the
    exact source/artifact audit, probe-output comparison, and the pinned ZDS
    negative control. The author then completed the fresh interactive emulator
    gate successfully and explicitly approved committing and pushing this
    batch.
