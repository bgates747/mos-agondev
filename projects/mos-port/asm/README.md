# Assembly port area

This directory is reserved for reviewed GNU-as sources or a deterministic
conversion pipeline. The current study does not copy converted upstream
assembly here because a broad mechanical rewrite without semantic review would
overstate the port's maturity.

The inventory and conversion constraints are recorded in the feasibility study;
the corresponding work is tracked only by `PORT-101` in the root TODO.
